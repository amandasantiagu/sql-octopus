meta {
  name: delete
  type: http
  seq: 5
}

delete {
  url: {{URL_DOMAIN}}/content/{{contentId}}
  body: none
  auth: inherit
}

headers {
  Authorization: {{TOKEN}}
}

vars:pre-request {
  contentId: 
}
