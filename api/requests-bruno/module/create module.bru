meta {
  name: create module
  type: http
  seq: 4
}

post {
  url: {{URL_DOMAIN}}/module
  body: json
  auth: inherit
}

headers {
  Authorization: {{TOKEN}}
}

body:json {
  {
    "label": "Módulo I"
  }
}
